# பங்களிப்பு / Contributing

பைந்தமிழின் மொழி அறிவு YAML கோப்புகளிலும் ANTLR இலக்கணங்களிலும் உள்ளது. புணர்ச்சி விதியோ மாத்திரைச்
சான்றோ சேர்க்க Python தெரிய வேண்டியதில்லை. The language knowledge of pytamil lives in YAML files
and ANTLR grammars; adding a sandhi rule or a mora example needs no Python.

## புணர்ச்சி விதி சேர்த்தல் / Adding a sandhi rule

கோப்பு: `pytamil/தமிழ்/resources/புணர்ச்சிவிதிகள்.yaml`. ஒரு விதி:

```yaml
ணகர, னகர ஈறுகள் இயல்பாதலும் திரிதலும்:
  - விதி: (...)('ண்') +வேற்றுமை+ (வல்லினம்)(...) = நிலைமொழி|திரிதல்('ட்').வருமொழி
  - சான்று:
    - மண் +வேற்றுமை+ குடம் = மட்குடம்
```

- இடப்பக்கம்: நிலைமொழி, வருமொழி பொருந்து வடிவங்கள். `(...)` எதுவும்; `(உயிர்)` `(மெய்)` `(வல்லினம்)` போன்ற
  வகைப் பெயர்கள் (`எழுத்து.yaml`-இல் உள்ளவை); `(இ,ஈ,ஐ)` எழுத்துப் பட்டியல்; `('மீன்')` ஒரு சொல்.
- நடுவில்: `+` (இரு வழிக்கும்), `+வேற்றுமை+`, `+அல்வழி+`.
- வலப்பக்கம்: `நிலைமொழி`, `வருமொழி` என்ற சொற்களுக்கு `|வடிகட்டி` சேர்த்து; வடிகட்டிகள்: `உடம்படுமெய்(ய்)`,
  `இரட்டுதல்`, `திரிதல்('ட்')`, `ஈறுகெடல்`, `உயிர்கெடல்`, `முதலிரட்டுதல்`. இணைப்பு `+` (விளைவின் மேல் விதிகள்
  மீண்டும்), `+இயல்பு+` அல்லது `.` (இயல்பாகச் சேர்). காற்புள்ளியால் பல மாற்றங்கள் (சே + அடி = சேயடி , சேவடி).
- ஒரு சொல்லிணைக்கு பல விதிகள் பொருந்தினால் சிறப்புவிதி (மிகக் கட்டுப்பட்ட வடிவம்) பொதுவிதியை மறைக்கும்.
- **ஒவ்வொரு சான்றும் தானாகச் சோதனை ஆகும்** (`pytest pytamil/tests/test_புணர்ச்சி.py`). நன்னூல் நூற்பா
  எண்ணைக் குறிப்பில் தரவும். Full grammar of a rule line: `resources/புணர்ச்சிவிதிகள்.g4`;
  notes: `docs/புணர்ச்சிவிதிகள்.md`.

## மாத்திரைச் சான்று சேர்த்தல் / Adding a mora example

கோப்பு: `pytamil/தமிழ்/resources/மாத்திரை.yaml` (இரண்டாம் YAML ஆவணம்). `சான்று:` பட்டியலில்
`சொல்: எழுத்து:வகை எழுத்து:வகை ...` என ஒரு வரி; அதுவும் தானாகச் சோதனை ஆகும்.

## யாப்புச் சான்று சேர்த்தல் / Adding a metre example

`pytamil/tests/test_வெண்பா_ஆய்வு.py`, `test_ஆசிரியப்பா.py`, `test_சீர்.py`-இல் பாடலும் எதிர்பார்க்கும் சீர்
வாய்பாடுகளும். உள்ளீடு **சீர்ப் பிரிப்புப்படி** இடைவெளி இட்ட யாப்பு வடிவம்.

## இலக்கணம் மாற்றுதல் / Changing a grammar

`.g4` மாற்றிய பின் `bash codegen.sh` (Java தேவை) ஓட்டி, உருவான `codegen/` கோப்புகளையும் commit செய்க.
CI அவை இலக்கணத்தோடு ஒத்திருப்பதைச் சரிபார்க்கும்.

## நிரல் நெறி / Code conventions

- **மொழிக் கருத்துகள் தமிழில், நிரலாக்க நுட்பம் ஆங்கிலத்தில்.** Tamil for language concepts (எழுத்து, சீர்,
  தளை, விதி, ஆய்வு), English for programming mechanics (parser, tree, listener, config).
- `pytest -q` பச்சை; `pylint pytamil` 10/10.
- ஒரு function/class-இன் **முதல் statement ஒரே வரியில்** இருக்க வேண்டும் (பல வரிகளில் பரந்தால்
  pylint செயலிழக்கும் — தமிழ்ப் பெயர்களோடு astroid-இல் உள்ள பிழை; விளக்கம் `.pylintrc`-இல்).
- Pull requests are merged with merge commits (no squash) so the history stays traceable.
